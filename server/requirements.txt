# Beep Detection Server Requirements
# Pin numpy to 1.x for tflite-runtime compatibility
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
librosa>=0.10.0
soundfile>=0.12.0
flask>=2.3.0
# Use TFLite runtime for inference (no AVX required, smaller footprint)
tflite-runtime>=2.14.0
