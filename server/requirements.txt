# Beep Detection Server Requirements
# Pin numpy to 1.x for compatibility
numpy>=1.24.0,<2.0.0
scipy>=1.10.0
librosa>=0.10.0
soundfile>=0.12.0
flask>=2.3.0

# Ensemble Detector Dependencies (only for CPUs with AVX support)
# TensorFlow for YAMNet model and full training support (requires AVX)
# tensorflow>=2.14.0
# TensorFlow Hub for loading pre-trained models like YAMNet (requires AVX)
# tensorflow-hub>=0.15.0

# TFLite runtime for inference-only deployments (works on legacy CPUs without AVX)
tflite-runtime>=2.14.0
